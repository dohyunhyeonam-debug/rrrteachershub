rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write to all authenticated users for EduFlow academy operations
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
